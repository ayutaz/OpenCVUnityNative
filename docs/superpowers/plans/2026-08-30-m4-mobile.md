# M4 Mobile Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 独自 C ABI が Android arm64-v8a と iOS arm64 で成立することを確認し、モバイルの binary を全部入り package に載せて配れる状態にする。

**Architecture:** 既存の 3 platform は **「実行中の OS = 対象 platform」** を前提にしている（`Get-OpenCvPlatform` が `$IsWindows` 等から判定する）。モバイルは**クロスコンパイル**なので、この前提が崩れる。したがって M4 の前半は「対象 platform を host から切り離す」構成変更であり、後半がそれを使った Android / iOS のビルドと検証である。C ABI そのものは変えない —— 変わるのはビルド構成、package の中身、`WebCamTexture` を受ける C# の入口だけである。

**Tech Stack:** CMake 3.25+（Android NDK toolchain / iOS toolchain）、OpenCV 5.0.0（既存の allowlist 構成をクロス向けにビルド）、PowerShell 7、Unity 6000.3.16f1（IL2CPP）、GitHub Actions（ubuntu-24.04 / macos-14）

**Spec:** `docs/roadmap.md` の「M4 — Mobile」節（完了条件 9 件と非ゴール）

**状態（2026-08-31）: 実施済み。main に入っている**（PR #41 `5354658`、PR #42 `57f971e`）。
**ただし M4 は完了していない** —— 完了条件 9 件のうちいくつかが閉じていない（**件数をここに書かない。正本は `docs/roadmap.md` の M4 判定表である**）。
判定表は `docs/roadmap.md` の M4 節にある。**この計画を次に読む人へ**:

- **クロスビルドが緑になってから CI で 8 回落ちた。** 罠は `add-a-platform` skill に
  踏んだ順で並べてある。**この計画に書いていない罠がそちらに 8 件ある。**
- **PR 前の AI レビューが 37 件を出した。** Critical 2 件はどちらも「CI が緑のまま」
  隠れていた。**レビュアーの指摘 1 件は CI が否定した**（16 KB の linker flag を
  `_INIT` に変える指摘。変えると効かなくなり、実物の `p_align` が 4096 に落ちた）。
- **`release.yml` を PR でも空撃ちするようにしたら、さらに 3 件出た。** うち 2 件は
  レビュアー 2 人が読んでも出ず、走らせた瞬間に出た。
- **v0.3.0 は下書きで止めてある**（実機検証待ち）。

## Global Constraints

このリポジトリの不変条件は全タスクに掛かる。**タスクごとに再掲しないので、着手前にここを読むこと。**

- **C ABI が唯一の native contract。** `cv::Mat*` や STL 型を境界の外へ出さない。opaque handle と固定サイズ型のみ（`CLAUDE.md`「アーキテクチャの中核」）
- **例外を ABI の外へ伝播させない。** 公開 ABI 関数は `OCVU_TRY_BEGIN` / `OCVU_TRY_END` で囲む（例外の一覧は `native/src/ocvu_error.h`）
- **`ocvu_mat_handle` は常に native が所有する。** Unity のメモリを指す handle を返さない。借用は 1 回の ABI 呼び出しの内側で完結する（`docs/abi-ownership-and-versioning.md` §1）
- **`Runtime/Interop` と `Runtime/Core` は `UnityEngine` を参照してはならない。** UnityEngine 依存は `Runtime/UnityIntegration/`（別 asmdef）だけ。`tests/Managed/CvUnity.Runtime.Shim/` がビルドで機械的に強制する
- **非 ASCII を出力する PowerShell スクリプトは先頭に `[Console]::OutputEncoding = [System.Text.UTF8Encoding]::new()` を置く**（`.claude/hooks/check-powershell-encoding.sh` が検出する）
- **検査を足す・変えるときは `prove-a-check-works` skill の手順を踏む。** 壊して落ちることを見るまで、その検査は動くと言えない
- **ABI 関数を足す・変えるときは `add-abi-function` skill の手順を踏む**
- **CI はローカルと同一のコマンド（`tools/dev.ps1`）を呼ぶ。** CI 専用の手順を作らない。全 job に `timeout-minutes` を設定する
- **`git add -A` / `git add .` は hook が拒否する。** ファイルを名指しして add する
- **PR を出す前に AI レビューを行う**（`CLAUDE.md`「変更を main へ入れるまで」）。**書いていない別のエージェントに見せる**
- **Unity は 6000.3 系のみ。** `tests/UnityProject/ProjectSettings/ProjectVersion.txt` が正本で、workflow はそこから読む
- **必須チェックを増減したら `CLAUDE.md` の「機構として強制されていること」表と `README.md` の該当段落を直す**（リポジトリ内で二重に書かれている唯一の箇所）

## この計画が扱わないもの

**完了条件 9 件のうち 2 件は、この計画では閉じない。** 理由を先に書く。

| 条件 | なぜ閉じないか |
| --- | --- |
| **iOS 実機での smoke test** | 実機が要る。GitHub ホストの runner に端末は繋がっておらず、Apple の署名も要る。**Task 9 で「シミュレータまで」を CI に入れ、実機は手順書を残して人に渡す** |
| **lifecycle / memory pressure の検証** | 同上。実機でアプリを background に送り、メモリ警告を起こす操作が要る。**Task 9 の手順書に含める** |

**この 2 件を「満たした」と書かない。** `milestone-complete` skill の「満たしたが実証はしていない」欄に入れる。

---

## File Structure

**新規に作るもの**

| ファイル | 責任 |
| --- | --- |
| `cmake/toolchains/android-arm64.cmake` | Android NDK の toolchain 変数を 1 箇所に集める。`ANDROID_ABI` / `ANDROID_PLATFORM` / 16 KB アライメントの linker flag |
| `cmake/toolchains/ios-arm64.cmake` | iOS の toolchain。`CMAKE_SYSTEM_NAME=iOS`、`CMAKE_OSX_DEPLOYMENT_TARGET=15.0`、**静的ライブラリを作る** |
| `tools/verify-android-page-size.ps1` | `.so` の ELF program header を読み、LOAD segment の `p_align` が 16384 以上であることを検査する。**`readelf` に頼らず自前で ELF を読む**（`verify-plugin-portability.ps1` と同じ理由） |
| `tools/plugin-meta/android-arm64/Android/arm64-v8a/*.meta` | Android plugin の Import Settings |
| `tools/plugin-meta/ios-arm64/iOS/*.meta` | iOS plugin の Import Settings |
| `Packages/.../Runtime/UnityIntegration/WebCamTextureConverter.cs` | `WebCamTexture` から `CvMat` を作る入口 |
| `tests/UnityProject/Assets/Tests/EditMode/WebCamTextureConverterTests.cs` | 上の EditMode テスト |
| `docs/m4-device-verification.md` | 実機でやることの手順書（この計画が閉じない 2 件） |

**変更するもの**

| ファイル | 何を |
| --- | --- |
| `tools/OpenCvConfig.psm1` | `Get-OpenCvConfig -Platform` にモバイルを通す。**`Get-OpenCvPlatform` は変えない**（host 判定のままで正しい） |
| `tools/opencv-config.psd1` | `Toolchains` に `android-arm64` / `ios-arm64`、`PlatformCMakeArgs` にクロス用の flag |
| `CMakePresets.json` | `android-arm64-debug` / `ios-arm64-debug` の configure / build preset |
| `tools/dev.ps1` | `build` に `-Platform` を足す（省略時は従来どおり host） |
| `tools/pack-upm-tarball.ps1` | `$PlatformBinaries` / `$AllowedPluginFiles` にモバイル 2 つ |
| `tools/assemble-plugins.ps1` | `$Allowed` にモバイル 2 つ |
| `tools/tests/OpenCvConfig.Tests.ps1` | 新しい不変条件（下記各タスク） |
| `.github/workflows/build-opencv.yml` | matrix にモバイル 2 つ |
| `.github/workflows/ci-native.yml` | モバイルのクロスビルド job |
| `.github/workflows/release.yml` | matrix にモバイル 2 つ |
| `native/CMakeLists.txt` | iOS では静的ライブラリを作る分岐 |

---

## Task 1: 対象 platform を host から切り離す

**Files:**
- Modify: `tools/opencv-config.psd1`（`Toolchains` と `PlatformCMakeArgs`）
- Modify: `tools/OpenCvConfig.psm1`（`Get-OpenCvConfig` の platform 検証）
- Test: `tools/tests/OpenCvConfig.Tests.ps1`

**Interfaces:**
- Consumes: 既存の `Get-OpenCvConfig -Platform <name>` / `Get-OpenCvConfigHash -Config <hashtable>`
- Produces: `Get-OpenCvConfig -Platform 'android-arm64'` と `-Platform 'ios-arm64'` が構成を返す。返る hashtable の形は既存 3 platform と同じ（`Platform` / `Tag` / `Modules` / `Toolchain` / `CMakeArgs`）

- [ ] **Step 1: 失敗するテストを書く**

`tools/tests/OpenCvConfig.Tests.ps1` の末尾に足す:

```powershell
# --- モバイル platform が構成を引ける ---
#
# 既存の 3 platform は「実行中の OS = 対象 platform」だったが、モバイルは
# クロスコンパイルなので host と対象が一致しない。Get-OpenCvPlatform（host 判定）
# は変えず、Get-OpenCvConfig -Platform に対象を明示的に渡す形にする。
foreach ($mobile in @('android-arm64', 'ios-arm64')) {
    $cfg = Get-OpenCvConfig -Platform $mobile
    Assert-That ($cfg.Platform -eq $mobile) "Get-OpenCvConfig -Platform $mobile returns that platform"
    Assert-That ($null -ne $cfg.Toolchain) "$mobile has a toolchain"
    Assert-That ($cfg.Toolchain.Generator -eq 'Ninja') "$mobile builds with Ninja (生成物の配置を platform 間で揃える)"

    # **構成ハッシュが他 platform と衝突しない。** 衝突すると、Android の
    # artifact 名で iOS の木が展開されるような事故が静かに成立する。
    $hash = Get-OpenCvConfigHash -Config $cfg
    Assert-That ($hash -match '^[0-9a-f]{12}$') "$mobile hash is 12 lowercase hex characters"
}

$allPlatforms = @('windows-x64', 'macos-arm64', 'linux-x64', 'android-arm64', 'ios-arm64')
$hashes = @($allPlatforms | ForEach-Object { Get-OpenCvConfigHash -Config (Get-OpenCvConfig -Platform $_) })
Assert-That ((@($hashes | Sort-Object -Unique)).Count -eq $allPlatforms.Count) `
    "all $($allPlatforms.Count) platforms hash differently (衝突すると別 platform の artifact が展開される)"

# **未知の platform は黙って通さない。** 既定に倒すと、対応していない対象で
# 「Windows の構成で Android をビルドする」が静かに成立する。
$rejected = $false
try { Get-OpenCvConfig -Platform 'android-armv7' | Out-Null }
catch { $rejected = $true }
Assert-That $rejected 'an unknown platform is rejected, not defaulted'
```

- [ ] **Step 2: 落ちることを確認する**

Run: `pwsh -NoProfile -File ./tools/tests/OpenCvConfig.Tests.ps1`
Expected: FAIL — `Get-OpenCvConfig -Platform android-arm64` が「未知の platform」で throw する（`Toolchains` に無いため）

- [ ] **Step 3: `tools/opencv-config.psd1` に toolchain を足す**

`Toolchains` の `'linux-x64'` の後ろに足す:

```powershell
        <#
            Android。**クロスコンパイルなので host と対象が一致しない。**
            NDK の toolchain file を使う。NDK の場所は環境変数
            ANDROID_NDK_ROOT / ANDROID_NDK_HOME から探す（CI の runner は
            前者を設定する）。

            arm64-v8a だけを対象にする。x86_64（エミュレータ）を含めるかは
            roadmap の穴 #8 で決める —— この計画では Task 8 で決着させる。
        #>
        'android-arm64' = @{
            Generator    = 'Ninja'
            Architecture = 'arm64-v8a'
            BuildType    = 'Release'

            <#
                Unity 6.3 が Android の minSdk を 23 -> 25 に上げた
                （tests/UnityProject/ProjectSettings/ProjectSettings.asset）。
                それより低い API を対象にしても Unity 側が受け取らないので、
                25 に揃える。**この値は構成ハッシュに入る。**
            #>
            AndroidPlatform = 'android-25'
        }

        <#
            iOS。**共有ライブラリを作らない。** iOS はアプリの外から
            .dylib を読み込めないので、Unity は static library (.a) を
            受け取って IL2CPP のバイナリへ静的リンクし、P/Invoke は
            DllImport("__Internal") で解決する（CLAUDE.md「IL2CPP / AOT を
            前提とする」）。
        #>
        'ios-arm64' = @{
            Generator    = 'Ninja'
            Architecture = 'arm64'
            BuildType    = 'Release'

            # Unity 6.3 が iOS の target を 13.0 -> 15.0 に上げた。
            IosDeploymentTarget = '15.0'
        }
```

`PlatformCMakeArgs` にも足す（既存の `'macos-arm64'` の隣）:

```powershell
        'android-arm64' = @(
            '-DANDROID_ABI=arm64-v8a'
            '-DANDROID_PLATFORM=android-25'
            # **16 KB page size。** Android 15 以降を対象にするアプリは
            # Google Play 上で 16 KB に対応していなければならず、2027-02-01
            # から未対応の更新は公開できなくなる。**止まるのは利用者の
            # リリースである**（こちらの .so が利用者のアプリに入るため）。
            # NDK r28 以降は既定で 16 KB だが、明示しておく —— 既定に頼ると
            # NDK を下げたときに黙って壊れる。
            '-DCMAKE_SHARED_LINKER_FLAGS=-Wl,-z,max-page-size=16384'
        )

        'ios-arm64' = @(
            '-DCMAKE_SYSTEM_NAME=iOS'
            '-DCMAKE_OSX_ARCHITECTURES=arm64'
            '-DCMAKE_OSX_DEPLOYMENT_TARGET=15.0'
            # bitcode は Xcode 14 で廃止された。明示的に切っておかないと
            # 古い OpenCV の CMake が有効化しようとする。
            '-DCMAKE_XCODE_ATTRIBUTE_ENABLE_BITCODE=NO'
        )
```

- [ ] **Step 4: 通ることを確認する**

Run: `pwsh -NoProfile -File ./tools/tests/OpenCvConfig.Tests.ps1`
Expected: PASS（`all assertions passed`）

- [ ] **Step 5: 検査が本当に効くことを確かめる**

`prove-a-child-works` ではなく `prove-a-check-works` の手順。3 通り壊して、3 通りとも落ちること:

```bash
# (a) ハッシュ衝突の検査 — android の AndroidPlatform を消して ios と同形にする
# (b) 未知 platform の拒否 — Get-OpenCvConfig の検証を削る
# (c) Ninja の要求 — android の Generator を 'Unix Makefiles' に変える
```

各変更で `pwsh -NoProfile -File ./tools/tests/OpenCvConfig.Tests.ps1` が **exit 1** になることを確認し、`git checkout --` で戻す。
**戻す前にコミットしておくこと** —— 未コミットの変更ごと巻き戻す事故が起きる。

- [ ] **Step 6: コミット**

```bash
git add tools/opencv-config.psd1 tools/OpenCvConfig.psm1 tools/tests/OpenCvConfig.Tests.ps1
git commit -m "feat(m4): 対象 platform を host から切り離し、モバイル 2 つの構成を足す"
```

---

## Task 2: Android の OpenCV を CI がビルドする

**Files:**
- Modify: `.github/workflows/build-opencv.yml`
- Modify: `tools/opencv.ps1`（`-Platform` を受ける）
- Test: `tools/tests/OpenCvConfig.Tests.ps1`

**Interfaces:**
- Consumes: Task 1 の `Get-OpenCvConfig -Platform 'android-arm64'`
- Produces: `opencv-5.0.0-android-arm64-<hash>` という artifact 名で OpenCV の木が公開される。`opencv.ps1 restore -Platform android-arm64` がそれを取得する

- [ ] **Step 1: 失敗するテストを書く**

`tools/tests/OpenCvConfig.Tests.ps1` に足す:

```powershell
# --- build-opencv.yml が全 platform をビルドする ---
#
# **matrix から静かに漏れると、restore が「artifact が無い」で落ちる。**
# しかもそれは M4 のずっと後、モバイルをビルドしようとした人の手元で起きる。
$buildOpencv = Get-Content -LiteralPath (Join-Path $repoRoot '.github/workflows/build-opencv.yml') -Raw
foreach ($p in @('windows-x64', 'macos-arm64', 'linux-x64', 'android-arm64', 'ios-arm64')) {
    Assert-That ($buildOpencv -match "platform:\s*$([regex]::Escape($p))\b") `
        "build-opencv.yml builds OpenCV for $p"
}

# **artifact 名は platform ごとに違う。** 同じ名前で上書きすると、
# 最後にビルドした platform の木が全 platform に配られる（M3 の Release
# asset 名の衝突と同じ形）。
foreach ($p in @('android-arm64', 'ios-arm64')) {
    $name = Get-OpenCvArtifactName -Config (Get-OpenCvConfig -Platform $p)
    Assert-That ($name -like "*-$p-*") "the artifact name for $p embeds the platform"
}
```

- [ ] **Step 2: 落ちることを確認する**

Run: `pwsh -NoProfile -File ./tools/tests/OpenCvConfig.Tests.ps1`
Expected: FAIL — `build-opencv.yml builds OpenCV for android-arm64`

- [ ] **Step 3: `tools/opencv.ps1` に `-Platform` を足す**

`param()` に足す:

```powershell
    <#
        対象 platform。省略すると実行中の host（Get-OpenCvPlatform）。
        **モバイルはクロスコンパイルなので、明示しないと host の構成で
        ビルドしようとする。**
    #>
    [ValidateSet('windows-x64', 'macos-arm64', 'linux-x64', 'android-arm64', 'ios-arm64')]
    [string] $Platform
```

`Get-OpenCvConfig` を呼んでいる箇所を `Get-OpenCvConfig -Platform $Platform` に替える（`$Platform` が空なら既存の挙動）。

- [ ] **Step 4: `build-opencv.yml` の matrix に足す**

```yaml
          - platform: android-arm64
            runner: ubuntu-24.04
            # NDK は runner に入っている（ANDROID_NDK_ROOT）。
            # コンテナには入れない —— Linux の .so と違い、Android の .so は
            # NDK の sysroot に対してリンクするので、host の glibc は関係ない。
          - platform: ios-arm64
            runner: macos-14
```

`Build OpenCV` の step に `-Platform ${{ matrix.platform }}` を渡す。

- [ ] **Step 5: 通ることを確認する**

Run: `pwsh -NoProfile -File ./tools/tests/OpenCvConfig.Tests.ps1`
Expected: PASS

- [ ] **Step 6: 検査が効くことを確かめる**

`build-opencv.yml` から `android-arm64` の行を消して、上のテストが落ちることを見る。戻す。

- [ ] **Step 7: 実際に CI で走らせる**

```bash
gh workflow run build-opencv.yml --ref <branch>
gh run watch <id>
```

**成功しただけでは足りない。** artifact が実際に上がっていることと、中に `libopencv_core.a` があることを確かめる:

```bash
gh run download <id> --pattern "opencv-5.0.0-android-arm64-*" --dir /tmp/ocv
find /tmp/ocv -name "*.a" | head
```

- [ ] **Step 8: コミット**

```bash
git add .github/workflows/build-opencv.yml tools/opencv.ps1 tools/tests/OpenCvConfig.Tests.ps1
git commit -m "feat(m4): Android / iOS の OpenCV を CI がビルドする"
```

---

## Task 3: Android の native plugin をクロスビルドする

**Files:**
- Create: `cmake/toolchains/android-arm64.cmake`
- Modify: `CMakePresets.json`
- Modify: `tools/dev.ps1`（`build` に `-Platform`）
- Test: `tools/tests/OpenCvConfig.Tests.ps1`

**Interfaces:**
- Consumes: Task 2 の OpenCV artifact
- Produces: `Packages/com.ayutaz.opencv-unity-native/Runtime/Plugins/Android/arm64-v8a/libopencv_unity_native.so`

- [ ] **Step 1: 失敗するテストを書く**

```powershell
# --- CMakePresets が全 platform を持つ ---
$presets = Get-Content -LiteralPath (Join-Path $repoRoot 'CMakePresets.json') -Raw | ConvertFrom-Json
$names = @($presets.configurePresets | ForEach-Object { $_.name })
foreach ($p in @('android-arm64-debug', 'ios-arm64-debug')) {
    Assert-That ($names -contains $p) "CMakePresets.json has a configure preset named $p"
}

# **モバイルに ASan の preset を作らない。** クロス環境では動かないので、
# 「あるのに走らせていない」状態を作らない。
foreach ($p in @('android-arm64-asan', 'ios-arm64-asan')) {
    Assert-That (-not ($names -contains $p)) "there is no $p preset (クロス環境で ASan は走らせない)"
}
```

- [ ] **Step 2: 落ちることを確認する**

Run: `pwsh -NoProfile -File ./tools/tests/OpenCvConfig.Tests.ps1`
Expected: FAIL — `CMakePresets.json has a configure preset named android-arm64-debug`

- [ ] **Step 3: toolchain file を書く**

`cmake/toolchains/android-arm64.cmake`:

```cmake
# Android arm64-v8a の toolchain 変数を 1 箇所に集める。
#
# **NDK の toolchain file を include するだけにしない。** ANDROID_ABI などの
# 変数は NDK 側の toolchain を読み込む「前」に決まっていなければならないので、
# ここで設定してから include する。
#
# NDK の場所は環境変数から探す。CI の runner は ANDROID_NDK_ROOT を設定する。
if(NOT DEFINED ANDROID_NDK)
  if(DEFINED ENV{ANDROID_NDK_ROOT})
    set(ANDROID_NDK "$ENV{ANDROID_NDK_ROOT}")
  elseif(DEFINED ENV{ANDROID_NDK_HOME})
    set(ANDROID_NDK "$ENV{ANDROID_NDK_HOME}")
  else()
    message(FATAL_ERROR
      "Android NDK not found. Set ANDROID_NDK_ROOT or ANDROID_NDK_HOME. "
      "**探せなかったことを既定で埋めない** — 見つからないまま host 向けに "
      "ビルドすると、成功したように見えて中身が別物になる。")
  endif()
endif()

set(ANDROID_ABI "arm64-v8a" CACHE STRING "")
set(ANDROID_PLATFORM "android-25" CACHE STRING "")
# STL は共有ではなく静的に。共有だと libc++_shared.so を利用者のアプリに
# 同梱してもらう必要があり、他の native plugin と衝突しうる。
set(ANDROID_STL "c++_static" CACHE STRING "")

include("${ANDROID_NDK}/build/cmake/android.toolchain.cmake")
```

- [ ] **Step 4: `CMakePresets.json` に preset を足す**

`configurePresets` に:

```json
    {
      "name": "android-arm64-debug",
      "displayName": "Android arm64-v8a (NDK)",
      "generator": "Ninja",
      "binaryDir": "${sourceDir}/build/android-arm64-debug",
      "toolchainFile": "${sourceDir}/cmake/toolchains/android-arm64.cmake",
      "cacheVariables": {
        "OCVU_BUILD_TESTS": "OFF",
        "CMAKE_BUILD_TYPE": "Release"
      }
    },
```

**`OCVU_BUILD_TESTS` を OFF にする理由をコメントで残せない**（JSON にコメントが書けない）ので、`CMakePresets.json` の直下ではなく `docs/roadmap.md` の M4 節に 1 行書く: **クロスビルドした GoogleTest は host で実行できないため。L1 の代わりは実機の smoke test（Task 9）である。**

`buildPresets` にも `{ "name": "android-arm64-debug", "configurePreset": "android-arm64-debug" }` を足す。

- [ ] **Step 5: `dev.ps1 build` に `-Platform` を足す**

`param()` に `[string] $Platform` を足し、preset の導出を替える:

```powershell
# 従来は実行中の platform から機械的に導いていた。**モバイルはクロス
# コンパイルなので host と一致しない。** 明示されたらそちらを使う。
$targetPlatform = if ($Platform) { $Platform } else { Get-OpenCvPlatform }
$Preset = "$targetPlatform-debug"
```

`Copy-NativePluginForUnity` の `switch` にモバイルの出力先を足す:

```powershell
        'android-arm64' { 'Android/arm64-v8a' }
        'ios-arm64'     { 'iOS' }
```

- [ ] **Step 6: 通ることを確認する**

Run: `pwsh -NoProfile -File ./tools/tests/OpenCvConfig.Tests.ps1`
Expected: PASS

- [ ] **Step 7: 実際にクロスビルドする（CI）**

ローカル（Windows）には NDK が無いので CI で確かめる。`ci-native.yml` に job を足す:

```yaml
  android:
    name: Android arm64-v8a (cross-build)
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v7
      - name: Resolve OpenCV config hash
        id: opencv-config
        shell: pwsh
        run: |
          Import-Module ./tools/OpenCvConfig.psm1 -Force
          $hash = Get-OpenCvConfigHash -Config (Get-OpenCvConfig -Platform 'android-arm64')
          "config-hash=$hash" >> $env:GITHUB_OUTPUT
      - name: Cache OpenCV
        uses: actions/cache@v6
        with:
          path: third_party/opencv/${{ steps.opencv-config.outputs.config-hash }}
          key: opencv-${{ steps.opencv-config.outputs.config-hash }}
      - name: Restore OpenCV
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: ./tools/opencv.ps1 restore -Platform android-arm64
      - name: Build the native plugin
        shell: pwsh
        run: ./tools/dev.ps1 build -Platform android-arm64
      - name: Verify the 16 KB page size
        shell: pwsh
        run: ./tools/verify-android-page-size.ps1
      - name: Upload the plugin tree
        uses: actions/upload-artifact@v7
        with:
          name: plugin-android-arm64
          path: Packages/com.ayutaz.opencv-unity-native/Runtime/Plugins
          if-no-files-found: error
```

**`Verify the 16 KB page size` は Task 4 で作る。** この Task では step を書かず、Task 4 で足す。

- [ ] **Step 8: コミット**

```bash
git add cmake/toolchains/android-arm64.cmake CMakePresets.json tools/dev.ps1 tools/tests/OpenCvConfig.Tests.ps1 .github/workflows/ci-native.yml
git commit -m "feat(m4): Android arm64-v8a の native plugin をクロスビルドする"
```

---

## Task 4: 16 KB page size を検査する

**Files:**
- Create: `tools/verify-android-page-size.ps1`
- Modify: `.github/workflows/ci-native.yml`（step を有効にする）
- Test: `tools/tests/OpenCvConfig.Tests.ps1`

**Interfaces:**
- Consumes: Task 3 が置く `Runtime/Plugins/Android/arm64-v8a/libopencv_unity_native.so`
- Produces: `./tools/verify-android-page-size.ps1 [-PluginPath <path>]` が exit 0 / 1 を返す

**なぜ要るか**（`docs/roadmap.md` の M4 節から）: Android 15 (API 35) 以降を対象とするアプリは Google Play 上で 16 KB に対応していなければならず、**2027-02-01 から未対応の更新は公開できなくなる。止まるのは利用者のリリースである** —— こちらが配る `.so` が利用者のアプリに入るため。

- [ ] **Step 1: 失敗するテストを書く**

まず script 自身のテストを `tools/tests/OpenCvConfig.Tests.ps1` に書く。**合成した ELF で両方向を確かめる** —— 実物だけで試すと「通ったが検査していない」が区別できない。

```powershell
# --- 16 KB page size の検査が、両方向に動く ---
#
# **実物の .so だけで試さない。** 実物は通る側にしかならないので、
# 「検査が効いている」と「検査が空振りしている」が区別できない。
# 最小の ELF を 2 つ合成して、通る側と落ちる側の両方を見る。
function New-TestElf {
    param([Parameter(Mandatory)][string]$Path, [Parameter(Mandatory)][long]$PAlign)

    # ELF64 little-endian、program header 1 つ（PT_LOAD）だけの最小構成。
    $bytes = New-Object byte[] 0x80
    # e_ident
    0x7F,0x45,0x4C,0x46,2,1,1 | ForEach-Object -Begin { $i = 0 } -Process { $bytes[$i++] = $_ }
    [BitConverter]::GetBytes([uint16]3).CopyTo($bytes, 0x10)   # e_type = ET_DYN
    [BitConverter]::GetBytes([uint16]0xB7).CopyTo($bytes, 0x12) # e_machine = AArch64
    [BitConverter]::GetBytes([uint64]0x40).CopyTo($bytes, 0x20) # e_phoff
    [BitConverter]::GetBytes([uint16]0x38).CopyTo($bytes, 0x36) # e_phentsize
    [BitConverter]::GetBytes([uint16]1).CopyTo($bytes, 0x38)    # e_phnum
    # program header at 0x40
    [BitConverter]::GetBytes([uint32]1).CopyTo($bytes, 0x40)    # p_type = PT_LOAD
    [BitConverter]::GetBytes([uint64]$PAlign).CopyTo($bytes, 0x70) # p_align
    [IO.File]::WriteAllBytes($Path, $bytes)
}

$elfOk  = Join-Path ([IO.Path]::GetTempPath()) 'ocvu-align-ok.so'
$elfBad = Join-Path ([IO.Path]::GetTempPath()) 'ocvu-align-bad.so'
New-TestElf -Path $elfOk  -PAlign 16384
New-TestElf -Path $elfBad -PAlign 4096

& pwsh -NoProfile -File (Join-Path $repoRoot 'tools/verify-android-page-size.ps1') -PluginPath $elfOk | Out-Null
Assert-That ($LASTEXITCODE -eq 0) 'a 16384-aligned ELF passes the page size check'

& pwsh -NoProfile -File (Join-Path $repoRoot 'tools/verify-android-page-size.ps1') -PluginPath $elfBad 2>&1 | Out-Null
Assert-That ($LASTEXITCODE -ne 0) 'a 4096-aligned ELF FAILS the page size check (これが落ちないなら検査は無意味)'

Remove-Item $elfOk, $elfBad -Force -ErrorAction SilentlyContinue

# **ci-native が実際に呼んでいること。** script が在るだけでは走らない。
$ciNative = @(Get-Content -LiteralPath (Join-Path $repoRoot '.github/workflows/ci-native.yml') |
              Where-Object { $_ -notmatch '^\s*#' })
Assert-That (@($ciNative | Where-Object {
    $_ -match '^\s*(run:\s*)?(&\s+)?\./tools/verify-android-page-size\.ps1(\s|$)'
}).Count -eq 1) 'ci-native.yml runs verify-android-page-size.ps1 in exactly one step'
```

- [ ] **Step 2: 落ちることを確認する**

Run: `pwsh -NoProfile -File ./tools/tests/OpenCvConfig.Tests.ps1`
Expected: FAIL — script が存在しない

- [ ] **Step 3: 検査 script を書く**

`tools/verify-android-page-size.ps1`:

```powershell
#Requires -Version 7.0

<#
    Android の .so が 16 KB page size に対応していることを検査する。

    ## なぜ要るか

    Android 15 (API 35) 以降を対象とするアプリは Google Play 上で 16 KB に
    対応していなければならず、**2027-02-01 から未対応の更新は公開できなく
    なる**（https://developer.android.com/guide/practices/page-sizes）。
    **止まるのは利用者のリリースである** —— こちらが配る .so が利用者の
    アプリに入るため。だから配る側で検査する。

    ## なぜ readelf に頼らないか

    verify-plugin-portability.ps1 と同じ理由である。開発機に readelf が
    無いと検査できない、という穴を作らない。ELF の program header は
    固定長のレコードが並んでいるだけなので、自分で読む。

    ## 何を見るか

    PT_LOAD セグメントの p_align。これが 16384 以上なら 16 KB の page で
    マップできる。**4096 だと Android 15 の 16 KB 端末で読み込めない。**
#>
param(
    # 検査する .so。省略すると package の Android plugin。
    [string] $PluginPath
)

Set-StrictMode -Version Latest
$ErrorActionPreference = 'Stop'
[Console]::OutputEncoding = [System.Text.UTF8Encoding]::new()

$RequiredAlign = 16384

if (-not $PluginPath) {
    $repoRoot = Split-Path -Parent $PSScriptRoot
    $PluginPath = Join-Path $repoRoot `
        'Packages/com.ayutaz.opencv-unity-native/Runtime/Plugins/Android/arm64-v8a/libopencv_unity_native.so'
}

if (-not (Test-Path -LiteralPath $PluginPath)) {
    [Console]::Error.WriteLine("Android plugin が見つかりません: $PluginPath")
    [Console]::Error.WriteLine('先に ./tools/dev.ps1 build -Platform android-arm64 を実行すること。')
    exit 1
}

$bytes = [IO.File]::ReadAllBytes($PluginPath)

if ($bytes.Length -lt 64 -or
    $bytes[0] -ne 0x7F -or $bytes[1] -ne 0x45 -or $bytes[2] -ne 0x4C -or $bytes[3] -ne 0x46) {
    [Console]::Error.WriteLine("ELF ではありません: $PluginPath")
    exit 1
}
if ($bytes[4] -ne 2) {
    [Console]::Error.WriteLine('ELF64 ではありません（arm64-v8a は 64-bit）。')
    exit 1
}

# e_phoff (0x20, 8 bytes) / e_phentsize (0x36, 2) / e_phnum (0x38, 2)
$phoff     = [BitConverter]::ToUInt64($bytes, 0x20)
$phentsize = [BitConverter]::ToUInt16($bytes, 0x36)
$phnum     = [BitConverter]::ToUInt16($bytes, 0x38)

if ($phnum -eq 0) {
    [Console]::Error.WriteLine('program header がありません。共有ライブラリではない可能性があります。')
    exit 1
}

$loads = @()
for ($i = 0; $i -lt $phnum; $i++) {
    $off = [int]$phoff + $i * $phentsize
    if ($off + 0x38 -gt $bytes.Length) { break }
    $ptype = [BitConverter]::ToUInt32($bytes, $off)          # p_type
    if ($ptype -ne 1) { continue }                            # PT_LOAD だけ
    $align = [BitConverter]::ToUInt64($bytes, $off + 0x30)    # p_align
    $loads += [pscustomobject]@{ Index = $i; Align = $align }
}

# **0 件で緑にしない。** PT_LOAD を 1 つも読めていない状態を「違反なし」と
# 読むと、検査が丸ごと空振りする。
if ($loads.Count -eq 0) {
    [Console]::Error.WriteLine('PT_LOAD セグメントが 1 つも見つかりません。ELF の読み方が想定と違います。')
    exit 1
}

$bad = @($loads | Where-Object { $_.Align -lt $RequiredAlign })
if ($bad.Count -gt 0) {
    [Console]::Error.WriteLine(
        "16 KB page size に対応していません: $PluginPath")
    foreach ($b in $bad) {
        [Console]::Error.WriteLine("  PT_LOAD[$($b.Index)] p_align = $($b.Align)（$RequiredAlign 以上が要る）")
    }
    [Console]::Error.WriteLine(
        '2027-02-01 から、未対応のアプリは Google Play で更新を公開できない。')
    [Console]::Error.WriteLine(
        '止まるのは利用者のリリースである —— この .so が利用者のアプリに入るため。')
    exit 1
}

$minAlign = ($loads | Measure-Object -Property Align -Minimum).Minimum
Write-Host "==> $(Split-Path -Leaf $PluginPath): PT_LOAD $($loads.Count) 件、最小 p_align = $minAlign（>= $RequiredAlign）" -ForegroundColor Green
exit 0
```

- [ ] **Step 4: 通ることを確認する**

Run: `pwsh -NoProfile -File ./tools/tests/OpenCvConfig.Tests.ps1`
Expected: PASS（合成 ELF の両方向が期待どおり）

- [ ] **Step 5: `ci-native.yml` の step を有効にする**

Task 3 Step 7 で書いた `Verify the 16 KB page size` の step が、この時点で実在する script を呼ぶようになる。

- [ ] **Step 6: CI で実物に当てる**

`gh workflow run ci-native.yml` を叩き、`==> libopencv_unity_native.so: PT_LOAD ... 最小 p_align = 16384` が出ることを確認する。

**もし 4096 が出たら**、それは検査が正しく働いた結果である。`PlatformCMakeArgs` の linker flag が効いていないので、NDK の版と `-Wl,-z,max-page-size=16384` の位置を見直す。

- [ ] **Step 7: コミット**

```bash
git add tools/verify-android-page-size.ps1 tools/tests/OpenCvConfig.Tests.ps1 .github/workflows/ci-native.yml
git commit -m "feat(m4): Android の 16 KB page size を CI で検査する"
```

---

## Task 5: iOS の静的ライブラリをビルドする

**Files:**
- Create: `cmake/toolchains/ios-arm64.cmake`
- Modify: `native/CMakeLists.txt`
- Modify: `CMakePresets.json`
- Modify: `.github/workflows/ci-native.yml`
- Test: `tools/tests/OpenCvConfig.Tests.ps1`

**Interfaces:**
- Consumes: Task 2 の iOS 版 OpenCV artifact
- Produces: `Packages/com.ayutaz.opencv-unity-native/Runtime/Plugins/iOS/libopencv_unity_native.a`

**なぜ静的か**: iOS はアプリの外から `.dylib` を読み込めない。Unity は `.a` を受け取って IL2CPP のバイナリへ静的リンクし、P/Invoke は `DllImport("__Internal")` で解決する（`CLAUDE.md`「iOS は静的リンク + `DllImport("__Internal")`」）。

- [ ] **Step 1: 失敗するテストを書く**

```powershell
# --- iOS だけが静的ライブラリを作る ---
#
# **共有ライブラリを作っても iOS では読み込めない。** しかもビルドは成功
# するので、Unity に入れて初めて分かる —— v0.1.0 で踏んだ「ビルドできた」と
# 「動く」の距離そのものである。CMake の分岐が消えていないことを見る。
$nativeCMake = @(Get-Content -LiteralPath (Join-Path $repoRoot 'native/CMakeLists.txt') |
                 Where-Object { $_ -notmatch '^\s*#' })
Assert-That (@($nativeCMake | Where-Object { $_ -match 'iOS' -and $_ -match 'STATIC' }).Count -ge 1) `
    'native/CMakeLists.txt builds a STATIC library for iOS (共有ライブラリは iOS で読み込めない)'
```

- [ ] **Step 2: 落ちることを確認する**

Run: `pwsh -NoProfile -File ./tools/tests/OpenCvConfig.Tests.ps1`
Expected: FAIL

- [ ] **Step 3: toolchain file を書く**

`cmake/toolchains/ios-arm64.cmake`:

```cmake
# iOS arm64（実機）の toolchain。
#
# **シミュレータ向けではない。** シミュレータは arm64 でも別の sysroot
# （iphonesimulator）を使うので、同じ .a では動かない。Task 9 でシミュレータを
# 足すときは、この file を複製せず CMAKE_OSX_SYSROOT を変数にすること。
set(CMAKE_SYSTEM_NAME iOS)
set(CMAKE_SYSTEM_PROCESSOR arm64)
set(CMAKE_OSX_ARCHITECTURES arm64 CACHE STRING "")
set(CMAKE_OSX_DEPLOYMENT_TARGET "15.0" CACHE STRING "")
set(CMAKE_OSX_SYSROOT "iphoneos" CACHE STRING "")

# bitcode は Xcode 14 で廃止された。明示的に切る。
set(CMAKE_XCODE_ATTRIBUTE_ENABLE_BITCODE NO CACHE STRING "")

# クロスコンパイルなので、try_run 系の検査は実行できない。
set(CMAKE_TRY_COMPILE_TARGET_TYPE STATIC_LIBRARY)
```

- [ ] **Step 4: `native/CMakeLists.txt` に分岐を足す**

`add_library(opencv_unity_native SHARED ...)` を替える:

```cmake
# **iOS だけ静的ライブラリにする。**
#
# iOS はアプリの外から .dylib を読み込めない。Unity は .a を受け取って
# IL2CPP のバイナリへ静的リンクし、P/Invoke は DllImport("__Internal") で
# 解決する。**SHARED のままでもビルドは成功する**ので、Unity に入れて
# 初めて壊れる —— v0.1.0 の「ビルドできた ≠ 動く」と同じ形である。
if(CMAKE_SYSTEM_NAME STREQUAL "iOS")
  set(OCVU_LIBRARY_KIND STATIC)
else()
  set(OCVU_LIBRARY_KIND SHARED)
endif()

add_library(opencv_unity_native ${OCVU_LIBRARY_KIND}
  # 既存のソース一覧をそのまま
)
```

- [ ] **Step 5: `CMakePresets.json` に preset を足す**

```json
    {
      "name": "ios-arm64-debug",
      "displayName": "iOS arm64 (device)",
      "generator": "Ninja",
      "binaryDir": "${sourceDir}/build/ios-arm64-debug",
      "toolchainFile": "${sourceDir}/cmake/toolchains/ios-arm64.cmake",
      "cacheVariables": {
        "OCVU_BUILD_TESTS": "OFF",
        "CMAKE_BUILD_TYPE": "Release"
      }
    },
```

`buildPresets` にも同名を足す。

- [ ] **Step 6: 通ることを確認する**

Run: `pwsh -NoProfile -File ./tools/tests/OpenCvConfig.Tests.ps1`
Expected: PASS

- [ ] **Step 7: `ci-native.yml` に job を足す**

Task 3 の Android job と同じ形で、`runs-on: macos-14` / `-Platform ios-arm64`。**移植性の検査は掛けない**（あれは Linux の glibc の話で、iOS には当てはまらない）。代わりに `lipo -info` で architecture を確かめる:

```yaml
      - name: Check the architecture of the static library
        run: |
          lib=Packages/com.ayutaz.opencv-unity-native/Runtime/Plugins/iOS/libopencv_unity_native.a
          lipo -info "$lib"
          lipo -info "$lib" | grep -q 'arm64' || { echo "arm64 ではない"; exit 1; }
          # **共有ライブラリになっていないこと。** ar のアーカイブなら 0x21 3C
          # ("!<") で始まる。
          head -c 2 "$lib" | grep -q '!<' || { echo ".a ではない（静的ライブラリを作れていない）"; exit 1; }
```

- [ ] **Step 8: コミット**

```bash
git add cmake/toolchains/ios-arm64.cmake native/CMakeLists.txt CMakePresets.json tools/tests/OpenCvConfig.Tests.ps1 .github/workflows/ci-native.yml
git commit -m "feat(m4): iOS arm64 の静的ライブラリをビルドする"
```

---

## Task 6: `WebCamTexture` から `CvMat` を作る

**Files:**
- Create: `Packages/com.ayutaz.opencv-unity-native/Runtime/UnityIntegration/WebCamTextureConverter.cs`
- Create: `tests/UnityProject/Assets/Tests/EditMode/WebCamTextureConverterTests.cs`
- Modify: `docs/api-reference.md`

**Interfaces:**
- Consumes: `CvMat.Create(rows, cols, CvMatType)` / `CvMat.CopyFrom(IntPtr, int, int)`（既存）
- Produces:
  - `CvUnity.Unity.WebCamTextureConverter.ToMat(WebCamTexture) -> CvMat`
  - `CvUnity.Unity.WebCamTextureConverter.ToMat(WebCamTexture, ref Color32[]) -> CvMat`（毎フレーム用。配列を使い回す）

**なぜ要るか**（roadmap の穴 #5）: カメラ入力そのものは Unity 側から受ける方針を変えないが、**受け取る API が今は無い** —— `TextureConverter` は `Texture2D` の RGBA32 しか受け付けない。

**設計の要点**: `WebCamTexture` は `GetRawTextureData` を持たない（`Texture` であって `Texture2D` ではない）。取れるのは `GetPixels32()` か `GetPixels32(Color32[])` で、後者は配列を使い回せる。**毎フレーム呼ばれる前提なので、確保を呼ぶ側に持たせる版を必ず用意する**（`CLAUDE.md`「毎フレームの細かな境界呼び出しを避ける」）。

- [ ] **Step 1: 失敗するテストを書く**

`tests/UnityProject/Assets/Tests/EditMode/WebCamTextureConverterTests.cs`:

```csharp
using System;
using CvUnity;
using CvUnity.Unity;
using NUnit.Framework;
using UnityEngine;

/// <summary>
/// WebCamTexture から CvMat を作る経路のテスト。
///
/// **実カメラは使わない。** EditMode でカメラを開くと、権限の無い環境や
/// カメラの無い CI で必ず落ちる —— 「環境が理由で赤い」は「コードが理由で
/// 赤い」と区別できないので、レーンとして成立しない。
///
/// 代わりに Color32[] を直接受ける overload を検査する。WebCamTexture から
/// 取れるのはまさにその配列であり、**変換の中身はそこから先に全部ある。**
/// カメラを開く 1 行だけがテストの外に残る。
/// </summary>
public class WebCamTextureConverterTests
{
    [Test]
    public void NullWebCamTextureIsRejected()
    {
        Assert.Throws<ArgumentNullException>(() => WebCamTextureConverter.ToMat(null));
    }

    [Test]
    public void PixelsAreConvertedWithTheRowOrderFlipped()
    {
        // Unity のテクスチャは左下が原点、OpenCV の Mat は左上が原点である。
        // **そのまま写すと上下が反転した Mat ができる。** 2x2 で見分けが付く
        // ように、行ごとに違う色を置く。
        var pixels = new Color32[]
        {
            // Unity の 0 行目 = 画像の一番下
            new Color32(10, 0, 0, 255), new Color32(11, 0, 0, 255),
            // Unity の 1 行目 = 画像の一番上
            new Color32(20, 0, 0, 255), new Color32(21, 0, 0, 255),
        };

        using var mat = WebCamTextureConverter.ToMat(pixels, width: 2, height: 2);

        Assert.AreEqual(2, mat.Rows);
        Assert.AreEqual(2, mat.Cols);
        Assert.AreEqual(4, mat.Channels);

        var got = new byte[2 * 2 * 4];
        mat.CopyTo(got, mat.Cols * 4);

        // Mat の 0 行目には Unity の 1 行目（上）が来ていること。
        Assert.AreEqual(20, got[0], "Mat の先頭行は Unity の最終行（画像の上端）であること");
        Assert.AreEqual(21, got[4]);
        Assert.AreEqual(10, got[8], "Mat の 2 行目は Unity の 0 行目（画像の下端）であること");
        Assert.AreEqual(11, got[12]);
    }

    [Test]
    public void PixelCountMustMatchTheGivenSize()
    {
        var pixels = new Color32[3];
        var ex = Assert.Throws<ArgumentException>(
            () => WebCamTextureConverter.ToMat(pixels, width: 2, height: 2));
        StringAssert.Contains("4", ex.Message, "期待した画素数をメッセージに出すこと");
    }

    [Test]
    public void ZeroSizeIsRejected()
    {
        // WebCamTexture は Play() の直後、まだ最初のフレームが来ていない間は
        // width/height が 16 などの既定値を返し、実際の画素はまだ無い。
        // **0 を通すと native 側で 0 行 0 列の Mat ができて、後段が黙って
        // 何もしない。** ここで止める。
        Assert.Throws<ArgumentOutOfRangeException>(
            () => WebCamTextureConverter.ToMat(new Color32[0], width: 0, height: 0));
    }
}
```

- [ ] **Step 2: 落ちることを確認する**

Run: `pwsh ./tools/dev.ps1 test-unity-editmode`
Expected: FAIL — `WebCamTextureConverter` が存在せずコンパイルエラー

- [ ] **Step 3: 実装する**

`Packages/com.ayutaz.opencv-unity-native/Runtime/UnityIntegration/WebCamTextureConverter.cs`:

```csharp
using System;
using System.Runtime.InteropServices;
using UnityEngine;

namespace CvUnity.Unity
{
    /// <summary>
    /// WebCamTexture から CvMat を作る。
    ///
    /// **カメラを開くのは呼ぶ側の仕事である。** このプロジェクトはカメラ入力を
    /// 独自に実装しない（roadmap の M4 非ゴール）—— 権限の要求も、解像度の
    /// 選択も、Play/Stop も Unity 側の API でやってもらう。ここが引き受けるのは
    /// 「Unity が持っている画素を OpenCV が読める形にする」ところだけである。
    ///
    /// **なぜ Texture2D 経由にしないか。** WebCamTexture は Texture2D ではない
    /// ので GetRawTextureData を持たない。Texture2D に一度写すと、GPU から
    /// CPU への読み戻しが 2 回になる。GetPixels32 は 1 回で済む。
    /// </summary>
    public static class WebCamTextureConverter
    {
        /// <summary>
        /// 現在のフレームを新しい CvMat に写す。
        ///
        /// **毎フレーム呼ぶなら <see cref="ToMat(WebCamTexture, ref Color32[])"/>
        /// を使うこと。** こちらは呼ぶたびに幅 x 高さ分の Color32[] を確保する。
        /// </summary>
        public static CvMat ToMat(WebCamTexture texture)
        {
            if (texture == null) { throw new ArgumentNullException(nameof(texture)); }
            Color32[] buffer = null;
            return ToMat(texture, ref buffer);
        }

        /// <summary>
        /// 現在のフレームを新しい CvMat に写し、画素の受け皿を使い回す。
        ///
        /// <paramref name="buffer"/> が null か大きさ違いのときだけ確保し直す。
        /// **毎フレームの経路はこちらを使う** —— 640x480 でも 1 フレームあたり
        /// 1.2 MB の確保になり、GC を押し上げる（CLAUDE.md「毎フレームの細かな
        /// 境界呼び出しを避ける」）。
        /// </summary>
        public static CvMat ToMat(WebCamTexture texture, ref Color32[] buffer)
        {
            if (texture == null) { throw new ArgumentNullException(nameof(texture)); }

            var width = texture.width;
            var height = texture.height;

            // **最初のフレームが来る前を弾く。** Play() の直後、WebCamTexture は
            // width/height に 16 などの既定値を返し、画素はまだ無い。そのまま
            // 進むと「サイズは通るのに中身が黒い Mat」ができて、原因が
            // 分からなくなる。didUpdateThisFrame で見分ける。
            if (!texture.isPlaying)
            {
                throw new InvalidOperationException(
                    "WebCamTexture is not playing. Call Play() and wait for the first frame " +
                    "(check didUpdateThisFrame) before converting.");
            }

            var needed = width * height;
            if (buffer == null || buffer.Length != needed)
            {
                buffer = new Color32[needed];
            }
            texture.GetPixels32(buffer);

            return ToMat(buffer, width, height);
        }

        /// <summary>
        /// Color32 の並びを CvMat に写す。**行の順序を入れ替える。**
        ///
        /// Unity のテクスチャは**左下が原点**、OpenCV の Mat は**左上が原点**で
        /// ある。そのまま写すと上下が反転した Mat ができ、しかも
        /// **エラーにはならない** —— cvtColor も resize も問題なく動くので、
        /// 画面に出して初めて気づく。ここで入れ替えておく。
        /// </summary>
        public static CvMat ToMat(Color32[] pixels, int width, int height)
        {
            if (pixels == null) { throw new ArgumentNullException(nameof(pixels)); }
            if (width <= 0 || height <= 0)
            {
                throw new ArgumentOutOfRangeException(
                    nameof(width),
                    $"width and height must be positive; got {width}x{height}. " +
                    "A WebCamTexture reports its real size only after the first frame.");
            }
            if (pixels.Length != width * height)
            {
                throw new ArgumentException(
                    $"pixel count mismatch: got {pixels.Length}, {width}x{height} needs {width * height}",
                    nameof(pixels));
            }

            // Color32 は RGBA の順に 1 バイトずつ並ぶ。CvMatType.Bgra32 は
            // 4 チャネル 8 ビットで、チャネルの意味づけは呼ぶ側が持つ
            // （OpenCV の Mat 自体は「4 チャネル」としか知らない）。
            // 並べ替えが要るなら cvtColor を通すのが呼ぶ側の仕事である。
            var stride = width * 4;
            var flipped = new byte[stride * height];

            var handle = GCHandle.Alloc(pixels, GCHandleType.Pinned);
            try
            {
                var src = handle.AddrOfPinnedObject();
                for (var row = 0; row < height; row++)
                {
                    // Unity の最終行が Mat の先頭行になる。
                    var srcRow = height - 1 - row;
                    Marshal.Copy(IntPtr.Add(src, srcRow * stride), flipped, row * stride, stride);
                }
            }
            finally
            {
                handle.Free();
            }

            var mat = CvMat.Create(height, width, CvMatType.Bgra32);
            try
            {
                mat.CopyFrom(flipped, stride);
                return mat;
            }
            catch
            {
                mat.Dispose();
                throw;
            }
        }
    }
}
```

- [ ] **Step 4: 通ることを確認する**

Run: `pwsh ./tools/dev.ps1 test-unity-editmode`
Expected: PASS。件数が 16 から **20** に増える（新しい 4 件）

- [ ] **Step 5: 上下反転の検査が本当に効くことを確かめる**

`WebCamTextureConverter.cs` の `var srcRow = height - 1 - row;` を `var srcRow = row;` に変え、`PixelsAreConvertedWithTheRowOrderFlipped` が落ちることを見る。戻す。

**これを確かめる理由**: 反転はエラーにならないので、テストが実際に順序を見ていないと気づけない。

- [ ] **Step 6: `docs/api-reference.md` を更新する**

C# 公開 API の節に `WebCamTextureConverter` を足す。**「まだ無い機能」の一覧から `WebCamTexture` を消す**（`milestone-complete` skill が名指しで警告している箇所）。

- [ ] **Step 7: コミット**

```bash
git add Packages/com.ayutaz.opencv-unity-native/Runtime/UnityIntegration/WebCamTextureConverter.cs tests/UnityProject/Assets/Tests/EditMode/WebCamTextureConverterTests.cs docs/api-reference.md
git commit -m "feat(m4): WebCamTexture から CvMat を作れるようにする"
```

---

## Task 7: モバイルの binary を全部入り package に載せる

**Files:**
- Create: `tools/plugin-meta/android-arm64/Android/arm64-v8a/libopencv_unity_native.so.meta`（+ ディレクトリの `.meta` 2 つ）
- Create: `tools/plugin-meta/ios-arm64/iOS/libopencv_unity_native.a.meta`（+ ディレクトリの `.meta` 1 つ）
- Modify: `tools/pack-upm-tarball.ps1`
- Modify: `tools/assemble-plugins.ps1`
- Modify: `tests/UnityProject/Assets/Tests/EditMode/PluginGatingTests.cs`
- Test: `tools/tests/PackageRelease.Tests.ps1`

**Interfaces:**
- Consumes: Task 3 / Task 5 が置く plugin 木
- Produces: 全部入り tarball が **5 platform** 分の binary を持つ

**注意**（roadmap が名指ししている）: **`pack-upm-tarball.ps1` と `assemble-plugins.ps1` の両方が未知の platform を拒むので、platform を足すときは 2 か所を直すことになる。**

- [ ] **Step 1: 失敗するテストを書く**

`tools/tests/PackageRelease.Tests.ps1` の全部入り検査を 3 → 5 に替える:

```powershell
# **数を直書きしない。** $PlatformBinaries の件数から導く —— platform を
# 足したときに、片方だけ直して片方が古いまま、という状態を作らない。
$expectedPlatformCount = 5
Assert-That ($AllowedPluginFiles.Count -ge $expectedPlatformCount * 2) `
    "the allowlist covers $expectedPlatformCount platforms (binary + .meta)"

foreach ($p in @('windows-x64', 'macos-arm64', 'linux-x64', 'android-arm64', 'ios-arm64')) {
    Assert-That ($PlatformBinaries.ContainsKey($p)) "pack-upm-tarball knows the binary path for $p"
}
```

`assemble-plugins.ps1` 側の `$Allowed` も同様に検査する:

```powershell
$assembleText = Get-Content -LiteralPath (Join-Path $repoRoot 'tools/assemble-plugins.ps1') -Raw
foreach ($rel in @('Android/arm64-v8a/libopencv_unity_native.so', 'iOS/libopencv_unity_native.a')) {
    Assert-That ($assembleText -match [regex]::Escape($rel)) `
        "assemble-plugins.ps1 carries $rel (**2 か所直す** —— packer だけでは全部入りに入らない)"
}
```

- [ ] **Step 2: 落ちることを確認する**

Run: `pwsh -NoProfile -File ./tools/tests/PackageRelease.Tests.ps1`
Expected: FAIL

- [ ] **Step 3: `.meta` を作る**

`tools/plugin-meta/android-arm64/Android/arm64-v8a/libopencv_unity_native.so.meta`:

```yaml
fileFormatVersion: 2
guid: 8f2a1c4e5b6d7a8e9f0a1b2c3d4e5f60
PluginImporter:
  externalObjects: {}
  serializedVersion: 2
  iconMap: {}
  executionOrder: {}
  defineConstraints: []
  isPreloaded: 0
  isOverridable: 0
  isExplicitlyReferenced: 0
  validateReferences: 1
  platformData:
  - first:
      : Any
    second:
      enabled: 0
      settings:
        Exclude Android: 0
        Exclude Editor: 1
        Exclude Linux64: 1
        Exclude OSXUniversal: 1
        Exclude Win64: 1
        Exclude iOS: 1
  - first:
      Android: Android
    second:
      enabled: 1
      settings:
        CPU: ARM64
  - first:
      Any:
    second:
      enabled: 0
      settings: {}
  - first:
      Editor: Editor
    second:
      enabled: 0
      settings:
        DefaultValueInitialized: true
  userData:
  assetBundleName:
  assetBundleVariant:
```

**GUID は他の `.meta` と重複してはならない。** 既存 7 つと突き合わせて確かめる（Step 5 の検査が見る）。

iOS 用も同じ形で、`iOS: iOS` を `enabled: 1`、`AddToEmbeddedBinaries: false`、`CompileFlags:`、`FrameworkDependencies:` を置く。

- [ ] **Step 4: `pack-upm-tarball.ps1` と `assemble-plugins.ps1` に足す**

`$PlatformBinaries`:

```powershell
    'android-arm64' = 'Runtime/Plugins/Android/arm64-v8a/libopencv_unity_native.so'
    'ios-arm64'     = 'Runtime/Plugins/iOS/libopencv_unity_native.a'
```

`$AllowedPluginFiles` と `assemble-plugins.ps1` の `$Allowed` にも、binary / `.meta` / ディレクトリの `.meta` を足す。

- [ ] **Step 5: GUID の重複を検査する**

`tools/tests/PackageRelease.Tests.ps1` に足す:

```powershell
# **.meta の GUID が重複していないこと。**
#
# 重複すると Unity が片方を無視する。**どちらが無視されるかは決まっていない**
# ので、「ある環境では動くが別の環境では動かない」という最も追いにくい形になる。
$guids = @()
foreach ($m in Get-ChildItem (Join-Path $repoRoot 'tools/plugin-meta') -Recurse -Filter '*.meta') {
    $text = Get-Content -LiteralPath $m.FullName -Raw
    if ($text -match 'guid:\s*([0-9a-f]{32})') { $guids += $Matches[1] }
}
Assert-That ($guids.Count -ge 10) "plugin-meta has at least 10 .meta files with a guid (saw $($guids.Count))"
Assert-That ((@($guids | Sort-Object -Unique)).Count -eq $guids.Count) `
    'every .meta guid is unique (重複すると Unity が片方を黙って無視する)'
```

- [ ] **Step 6: `PluginGatingTests.cs` を 5 platform に広げる**

`expected` の辞書に足す:

```csharp
            { "libopencv_unity_native.so", "Linux" },      // 既存
            // Android / iOS はエディタでは有効にならない。GetEditorData("OS") は
            // 空文字を返す —— **エディタ向けの振り分けを持たない**ことが正しい。
```

`EachPluginIsEnabledOnlyForItsOwnStandaloneTarget` の `expected` に `BuildTarget.Android` / `BuildTarget.iOS` を足し、**`native plugins present: 3` を要求している箇所を 5 に替える**（`ci-unity.yml` の `requireOutput` と `dev.ps1` の両方）。

- [ ] **Step 7: 通ることを確認する**

Run: `pwsh -NoProfile -File ./tools/tests/PackageRelease.Tests.ps1`
Expected: PASS

- [ ] **Step 8: コミット**

```bash
git add tools/plugin-meta tools/pack-upm-tarball.ps1 tools/assemble-plugins.ps1 tools/tests/PackageRelease.Tests.ps1 tests/UnityProject/Assets/Tests/EditMode/PluginGatingTests.cs
git commit -m "feat(m4): モバイルの binary を全部入り package に載せる"
```

---

## Task 8: 対応 CPU アーキテクチャを決める

**Files:**
- Modify: `docs/roadmap.md`（M4 節に決定を書く）
- Modify: `README.md`（対応 platform の表）

**Interfaces:** なし（決定の記録）

**何を決めるか**（roadmap の穴 #8）: 少なくとも **Android の x86_64（エミュレータ）を含めるか**。開発体験に直接効くので明示的に決める。

- [ ] **Step 1: 材料を集める**

次を実測して書き出す。**推測で決めない。**

```bash
# (a) x86_64 を足すと OpenCV のビルドが何分増えるか
#     -> build-opencv.yml の Android job の所要時間を見る
# (b) 全部入り tarball が何バイト増えるか
#     -> Android arm64 の .so のサイズから見積もり、実測で確かめる
# (c) 512 MB の上限にどれだけ近づくか
```

- [ ] **Step 2: 決定を書く**

`docs/roadmap.md` の M4 節に:

```markdown
### 対応 CPU アーキテクチャの決定（穴 #8）

**Android は arm64-v8a のみ。x86_64（エミュレータ）は含めない。**

理由:

- **実機は事実上すべて arm64-v8a である。** x86_64 が要るのはエミュレータでの
  開発中だけで、配る成果物の対象ではない
- **含めると全部入りが大きくなる。** 実測 <値> —— OpenUPM の 512 MB 上限に
  対する余裕は減る（現在 9.6 MB）
- **エミュレータでの開発を止めるわけではない。** 利用者が自分でビルドする経路は
  残る（`CMakePresets.json` に preset を足せばよい）

**この決定は覆せる。** x86_64 を求める声が実際に出たら、`Toolchains` に
`android-x64` を足すだけで通る形にしてある（Task 1 の構成が platform を
host から切り離しているため）。

**iOS は arm64 のみ。** シミュレータ（x86_64 / arm64-simulator）は別の sysroot
なので同じ `.a` では動かない。**シミュレータを配る予定は無い** —— 実機で
動かすためのパッケージである。
```

- [ ] **Step 3: `README.md` を直す**

対応 platform の表に Android arm64-v8a と iOS arm64 を足し、**含めないものも書く**（x86_64、シミュレータ）。

- [ ] **Step 4: コミット**

```bash
git add docs/roadmap.md README.md
git commit -m "docs(m4): 対応 CPU アーキテクチャを決める（Android arm64-v8a のみ / iOS arm64 のみ）"
```

---

## Task 9: Windows IL2CPP の結論と、実機検証の手順書

**Files:**
- Create: `docs/m4-device-verification.md`
- Modify: `docs/roadmap.md`（Windows IL2CPP の結論）
- Modify: `CLAUDE.md`（「CI が保証していないこと」の更新）

**Interfaces:** なし（検証と記録）

**roadmap が名指ししている制約**: 「**`windows-2022` 上で `game-ci/unity-test-runner@v4` へ実際に投げ、その run の出力を根拠として記録したうえで**、Windows の IL2CPP Player を CI で回すかどうかを結論として書く（穴 #7）。やると決めるか諦めると決めるかは問わないが、**上流の issue を読んだ結果を根拠にしない** —— 今回崩れた判断がたどったのがその経路である」

- [ ] **Step 1: 実際に投げる**

ブランチに一時的な workflow を足して `windows-2022` で `game-ci/unity-test-runner@v4` を走らせる:

```yaml
# .github/workflows/probe-windows-unity.yml （**この Task の最後に消す**）
name: probe-windows-unity
on: workflow_dispatch
jobs:
  probe:
    runs-on: windows-2022
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v7
      - uses: game-ci/unity-test-runner@v4
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
        with:
          projectPath: tests/UnityProject
          testMode: EditMode
          artifactsPath: artifacts/probe
          githubToken: ${{ secrets.GITHUB_TOKEN }}
```

```bash
gh workflow run probe-windows-unity.yml --ref <branch>
gh run watch <id>
gh run view <id> --log > /tmp/probe.log
```

- [ ] **Step 2: 出力を根拠として記録する**

`docs/roadmap.md` の M4 節に、**run の実際の出力を引用して**結論を書く。成功しても失敗しても書く。

```markdown
### Windows の IL2CPP を CI で回すか（穴 #7）

**2026-xx-xx に `windows-2022` で `game-ci/unity-test-runner@v4` へ実際に投げた**
（run <id>）。結果:

<実際の出力を引用>

**結論: <やる / やらない>。** 根拠は上の run の出力である。
**上流の issue は根拠にしていない** —— 2026-08-29 に崩れた判断が
たどったのがその経路だからである。
```

- [ ] **Step 3: probe workflow を消す**

```bash
git rm .github/workflows/probe-windows-unity.yml
```

**残さない。** 「一時的に足した workflow が居座って、誰も走らせないまま
必須でないチェックとして PR に出続ける」状態を作らない。

- [ ] **Step 4: 実機検証の手順書を書く**

`docs/m4-device-verification.md`。**この計画が閉じない 2 件を、人が実行できる形にする。**

```markdown
# M4 実機検証の手順

**この文書は、CI では閉じられない完了条件を人が実行するためのものである。**

閉じられない理由: GitHub ホストの runner に端末は繋がっておらず、iOS は
Apple の署名も要る。**「実機で確認した」と書けるのは、実際に端末で走らせた
人だけである。**

## 1. iOS 実機の smoke test

### 前提
- Apple Developer アカウント（署名のため）
- iOS 15.0 以降の実機
- macOS + Xcode

### 手順
1. `./tools/dev.ps1 build -Platform ios-arm64` で `.a` を作る
2. `tests/UnityProject` を iOS 向けに Build（IL2CPP、Managed Stripping Level = High）
3. 出た Xcode プロジェクトを開き、署名して実機へ流す
4. **確かめること**:
   - `ocvu_get_abi_version()` が 1 を返す（P/Invoke が `__Internal` で解決している）
   - `CvOps.CvtColor` が動く（OpenCV が静的リンクされている）
   - **stripping 後も落ちない** —— Managed Stripping Level を High にしても
     P/Invoke 宣言が消えていないこと。`link.xml` が効いているかはここでしか分からない

### 記録すること
- Unity の版、Xcode の版、端末の機種と iOS の版
- 上の 3 つそれぞれの結果
- **落ちたなら、落ちた場所とスタック**

## 2. lifecycle と memory pressure

### 手順
1. 上の smoke test が通ったビルドを使う
2. **background / foreground**: アプリを Home に送り、30 秒待って戻す。
   `CvMat` を保持したまま行い、戻った後に `CopyTo` が成功すること
3. **memory pressure**: Xcode の Debug > Simulate Memory Warning を送る。
   保持していた `CvMat` が無効化されていないこと（native が所有しているので、
   Unity の GC やメモリ警告では解放されない —— **それを確かめる**）

### 記録すること
- 各操作の後で ABI 呼び出しが成功したか
- **落ちたなら、それは所有権契約の反例である。**
  `docs/abi-ownership-and-versioning.md` §1 を見直すこと

## 3. Android 実機（16 KB 端末）

CI は `.so` の `p_align` を見るが、**実際に 16 KB page の端末で読み込めるかは
見ていない。** Pixel 8 以降 + Android 15 で確かめること。

`adb logcat` に `DllNotFoundException` が出ないこと。
```

- [ ] **Step 5: `CLAUDE.md` の「CI が保証していないこと」を更新する**

mobile の行を、**何が閉じて何が閉じていないか**に書き換える。

- [ ] **Step 6: コミット**

```bash
git add docs/m4-device-verification.md docs/roadmap.md CLAUDE.md
git rm .github/workflows/probe-windows-unity.yml
git commit -m "docs(m4): Windows IL2CPP の結論を実測で書き、実機検証の手順を残す"
```

---

## Task 10: リリース経路にモバイルを載せる

**Files:**
- Modify: `.github/workflows/release.yml`
- Modify: `tools/tests/OpenCvConfig.Tests.ps1`
- Modify: `CLAUDE.md`（asset 件数）
- Modify: `.github/release-notes.md`

**Interfaces:**
- Consumes: Task 3 / 5 / 7
- Produces: Release の asset が **5 platform** 分になる

- [ ] **Step 1: 失敗するテストを書く**

```powershell
# --- release.yml が 5 platform をパッケージする ---
$release = Get-Content -LiteralPath (Join-Path $repoRoot '.github/workflows/release.yml') -Raw
foreach ($p in @('windows-x64', 'macos-arm64', 'linux-x64', 'android-arm64', 'ios-arm64')) {
    Assert-That ($release -match "platform:\s*$([regex]::Escape($p))\b") `
        "release.yml packages $p"
}
```

- [ ] **Step 2: 落ちることを確認する**

Run: `pwsh -NoProfile -File ./tools/tests/OpenCvConfig.Tests.ps1`
Expected: FAIL

- [ ] **Step 3: `release.yml` を直す**

matrix に 2 つ足し、**staged asset の数を数え直す**:

```
5 platform x (tarball 1 + 配布物 4) = 25
+ 全部入りの tarball 1 + その checksums.txt 1
= 27
```

`if ($staged.Count -ne 17)` を **27** に替える。`SHA256SUMS.txt` を足して Release には **28 件**。

**移植性の検査の振り分けも直す** —— `tools/tests/OpenCvConfig.Tests.ps1` の
「配る binary を作る job が移植性を検査し、その振り分けが matrix の platform 集合と
過不足なく一致する」検査が、新しい 2 platform を「未決定」として落とす。
`linux-x64` だけ検査し、**Android / iOS は明示的に skip** する（あれは glibc の話である）。

- [ ] **Step 4: 通ることを確認する**

Run: `pwsh -NoProfile -File ./tools/tests/OpenCvConfig.Tests.ps1`
Expected: PASS

- [ ] **Step 5: 空撃ちで通す**

```bash
gh workflow run release.yml --ref <branch>
gh run watch <id>
```

**`==> staged 27 assets` が出ること。** 出なければ数が合っていない。

- [ ] **Step 6: 件数を書いている箇所を全部直す**

```bash
grep -rn "17 件\|18 件\|3 platform" --include=*.md . | grep -v third_party
```

`CLAUDE.md` の `release.yml` の行、`docs/roadmap.md` の workflow 表、`.github/release-notes.md`。

- [ ] **Step 7: コミット**

```bash
git add .github/workflows/release.yml tools/tests/OpenCvConfig.Tests.ps1 CLAUDE.md docs/roadmap.md .github/release-notes.md
git commit -m "feat(m4): リリース経路にモバイル 2 platform を載せる"
```

---

## Task 11: 完了判定と PR

**Files:**
- Modify: `docs/roadmap.md`（M4 の判定表）
- Modify: `CLAUDE.md`（現在地）
- Modify: `docs/README.md`

- [ ] **Step 1: `milestone-complete` skill の手順を踏む**

完了条件 8 件を 1 件ずつ実測で照合する。**「満たした / 満たしていない / 満たしたが実証していない」の 3 欄**で書く。

- [ ] **Step 2: 全レーンを回す**

```bash
pwsh ./tools/dev.ps1 test
pwsh ./tools/dev.ps1 test-asan
pwsh ./tools/dev.ps1 test-unity-editmode
pwsh ./tools/dev.ps1 test-unity-tarball
```

**`test-unity-tarball` は実行したコマンドラインをそのまま報告に貼る** —— `-PluginSource` が抜けていても出力からは分からない（`milestone-complete` skill が名指しで警告している）。

- [ ] **Step 3: 文書の陳腐化を確認する**

`CLAUDE.md` / `README.md` / `docs/roadmap.md` / `docs/README.md` / `docs/api-reference.md` /
`docs/abi-ownership-and-versioning.md`。**数えている数を全部洗う**（公開 ABI の本数、asset 件数、EditMode の件数、platform 数）。

- [ ] **Step 4: AI レビューを受ける**

**この差分を書いていない別のエージェント**に、ブランチ全体を見せる。指摘を裁定し、修正差分をスコープを絞って再レビューする。

- [ ] **Step 5: PR を出す**

本文に書くこと:
- 何を成立させたか（実測値つき）
- **閉じていない 2 件**（iOS 実機、lifecycle / memory pressure）と、その理由
- レビューで何が指摘され、どう直したか
- 意図的に見送ったもの

- [ ] **Step 6: CI が全パスしたらマージ**

```bash
gh pr checks <n> --watch --fail-fast
```

**新しい job（Android / iOS）を必須チェックにするかを決めるまでが作業である**（`CLAUDE.md`「機構として強制されていること」）。決めたら表を直す。

---

## Self-Review

**1. Spec coverage** — roadmap の M4 完了条件 8 件との対応:

| 完了条件 | Task |
| --- | --- |
| Android / iOS の native artifact を CI が生成する | 2, 3, 5 |
| 16 KB page size を CI で検証する | 4 |
| iOS の static link と stripping 後の P/Invoke（**実機**） | 5（ビルド）+ 9（手順書）。**実機は閉じない** |
| lifecycle / memory pressure | 9（手順書）。**閉じない** |
| `WebCamTexture` から `CvMat` | 6 |
| macOS の Plugin Import Settings を Unity で実測 | 5 の CI job が macos-14 で走るので、そこに EditMode を足す（Task 5 Step 7 に含む） |
| Windows IL2CPP の結論 | 9 |
| 対応 CPU アーキテクチャの決定 | 8 |
| モバイル binary が全部入りに入り `test-unity-tarball` が通る | 7, 11 |

**2. Placeholder scan** — 「TBD」「後で」「適切に」は無い。Task 9 の Step 2 だけ run id と出力が空欄だが、**それは実行してからでないと書けない値**であり、プレースホルダではない。

**3. Type consistency** — `WebCamTextureConverter.ToMat` の 3 つの overload は Task 6 内で一貫している。`verify-android-page-size.ps1` の `-PluginPath` は Task 4 のテストと Task 3 の workflow で同じ名前。`Get-OpenCvConfig -Platform` は Task 1 で定義し、Task 2 / 3 / 5 が同じ形で使う。
